{
  "name": "jenkins-docker-project",
  "version": "1.0.0",
  "description": "A simple Node.js app for Jenkins and Docker",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "http": "^0.0.1-security"
  }
}

